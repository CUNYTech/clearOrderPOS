const outerBox = {
  boxSizing: 'border-box',
  margin: 'auto',
  width: '75%',
  height: 'auto',
  padding: '20px',
  overflow: 'auto',
  maxHeight: '100%',
}

const cardStyle = {
  height: 'auto',
  maxHeight: '100%',
  padding: '20px',
  textAlign: 'center',
  backgroundColor: 'white',
  margin: '10px 0px',
  width: '100%',
  overflow: 'auto',
}

const flexColumn = {
  display: 'flex',
  flexDirection: 'row',
}

const column = {
  margin: 'auto',
}

const outerBusiness = {
  boxSizing: 'border-box',
  border: '5px solid transparent',
  borderRadius: '15px',
  margin: '0px',
  padding: '0px',
  width: '100%',
  height: '100%',
  height: '100vh',
  maxHeight: '100%',
  minHeight: '100%',
  display: 'flex',
  flexDirection: 'row',
}

const pane = {
  boxSizing: 'border-box',
  border: '5px solid transparent',
  borderRadius: '15px',
  height: 'auto',
  padding: '0px',
  margin: '0px',
  textAlign: 'center',
  width: '50%',
  minWidth: '50%',
  maxWidth: '50%',
  overflow: 'auto',
}

const wideButton ={
  margin: 'auto',
  width: '75%',
}

const appBarStyle = {
  border: '0px solid transparent',
  borderRadius: '15px',
}

export {
  outerBox,
  cardStyle,
  flexColumn,
  column,
  outerBusiness,
  pane,
  wideButton,
  appBarStyle,
}
